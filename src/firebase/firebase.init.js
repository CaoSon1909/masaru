import { initializeApp } from "firebase/app";
import firebaseConfig from './firebase.config';

function firebaseInitialization() {
    // initializeApp(firebaseConfig, "Masaru")
    return;
}

export default firebaseInitialization;